services:
  - type: web
    name: attendance
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python attendence_server.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.13
